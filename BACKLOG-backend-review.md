# Back-end review, 2026-08-23: what was found, what was fixed, what waits

17 agents, five dimensions, every high-severity finding adversarially
verified by reproduction. 12 confirmed, 0 refuted. All 12 are FIXED as of
this commit. The medium/low findings below are the backlog, verbatim.

## Fixed (the 12 confirmed)

- **[pipeline]** refresh.py catches only AtsError; 15 reproduced fetcher/classifier exceptions escape and kill the 6am run before anything is written
- **[pipeline]** Sanity gate self-disarms one day after a breakage: build_board writes the history snapshot unconditionally, so day 2 compares broken-vs-broken and publishes the bad board
- **[admin]** GET API endpoints and server startup crash outright when submissions.json is '{}'
- **[admin]** POST /api/merge with keep == drop silently deletes the company
- **[data]** Same company id present in BOTH companies.json and suppliers.json: 2
- **[data]** Duplicate ids WITHIN suppliers.json: 3 ids, each appearing twice
- **[data]** board.postings ids non-unique: 316 ids duplicated, 668 surplus rows
- **[intake]** Command injection: issue-body URL reaches the shell via ${{ }} interpolation in add-company.yml
- **[intake]** promote_candidates creates duplicate '-N' company because norm() keeps parentheticals but kebab() strips them
- **[intake]** Non-ASCII (CJK) names silently dropped by conference_intake and refused/mis-deduped by promote_candidates
- **[consistency]** build_site.py strips two fields index.html actually reads; public site shows 16 instead of 969
- **[consistency]** Admin submission-approve writes hiring without a 'roles' key; refresh.py --company then crashes

## Backlog (medium/low, not yet addressed)

### [pipeline/medium] MAX_HIRING_DROP half of the sanity gate is dead code: nothing ever writes meta.json['companies_hiring']
scripts/build_site.py:119 reads m.get("companies_hiring") from data/meta.json, but grep across scripts/ and .github/ shows no writer — scripts/refresh.py:148 rewrites meta.json daily with exactly {last_run, previous_run, counts, method} (current data/meta.json confirms the key is absent), so `was` is always None and the check is skipped silently. Reproduced: a board whose companies-with-openings collapsed to 1 (with postings count steady) passes sanity_check with no objections, though MAX_HIRING_DROP=0.40 exists precisely to catch that. Either refresh.py should write companies_hiring (e.g. cou

### [pipeline/medium] A control character in any fetched job title crashes export_xlsx with IllegalCharacterError — after the data commit, and it takes the day's AE alert down with it
Reproduced end-to-end: classify.rollup([{"title": "Account Executive \x0b SLED", ...}]) → note carries the \x0b verbatim → refresh.py stores it in companies.json hiring.note → export_xlsx.py:38 hire_text → openpyxl raises IllegalCharacterError on ws.cell(). In refresh.yml the 'Regenerate spreadsheet' step runs after the data commit (so data survives, by design) but BEFORE 'Open an issue for new AE openings' — the crash skips the alert step, so the exact day a new AE title appears with a stray control char is the day its alert is silently lost. The bad note also persists in companies.json, so e

### [pipeline/low] validate()/selftest guarantee hiring.status only, but refresh.py indexes hiring['note'] and hiring['roles'] directly
scripts/refresh.py:113 — `{k: comp["hiring"][k] for k in ("status", "note", "roles")}` in the non-target branch. Reproduced: a company with hiring={"status": "Unknown"} passes both admin.py:99 validate() (checks only truthiness of the block and status membership) and selftest.py, then raises KeyError 'note' here. Only reachable on `--company` runs (the 6am run targets every company, and current data has all three keys everywhere), so this is the KeyError that bites the first time someone tests a single company after adding one by hand — same class of bug as the one that already killed today's 

### [pipeline/low] refresh.py writes its four output files non-atomically in sequence; a mid-write kill leaves torn JSON the next run crashes on at startup
scripts/refresh.py:47 dump_json() is a plain open(path, 'w') (admin.py has write_atomic for exactly this reason but refresh doesn't use it), and lines 145-148 write hiring_history snapshot → companies.json → latest_diff.json → meta.json in order. A kill during the snapshot write leaves a truncated data/hiring_history/YYYY-MM-DD.json; on re-run, previous_snapshot(today) sees the file exists, load_json raises json.JSONDecodeError before any fetch, and --force does not help (the corrupt file is read as the diff baseline, not replaced first). In CI this self-heals because a crash prevents the comm

### [admin/medium] admin.html has no renderer or caller for the new 'blocked' queue and 'retry-board' action
scripts/admin.py:260-272 (q_blocked, in QUEUES/LABEL as 'Blocked boards') and :689-724 (act_retry_board) vs admin.html (mtime 17:03, zero grep hits for 'blocked' or 'retry-board'; RENDER has 11 keys, no RENDER.blocked). The nav is built from the server's META.labels (admin.html:181-185), so a 'Blocked boards' tab appears with 185 real items, but show('blocked') hits RENDER[key](it) at admin.html:247 where RENDER['blocked'] is undefined -> TypeError; the tab renders only the intro line and no items, and nothing in the UI can trigger /api/retry-board. Server drifted ahead of the UI (likely a par

### [admin/medium] act_capture, act_submit, act_vendor_scope raise unhandled exceptions on malformed/edge bodies (500 instead of 400)
Reproduced by direct calls: (a) act_capture with manual.json present as '{}' -> KeyError 'postings' at scripts/admin.py:962 (read() default only applies when the file is absent/corrupt); (b) act_capture with jobs=["Account Executive"] (strings, not objects) -> AttributeError 'str' object has no attribute 'get' in the job loop; (c) act_submit with submissions.json='{}' -> KeyError 'items' at :1020; (d) act_vendor_scope with name=123 -> AttributeError 'int' object has no attribute 'lower' in _vkey at :737. do_POST's catch-all converts these to HTTP 500 with the raw exception name (verified: POST

### [admin/medium] act_vendor_scope_all with 'names' as a string writes one permanent junk ruling per character and reports success
scripts/admin.py:397-424, loop at :410. Input {"names": "Okta", "call": "out"}: 'if not names' passes (non-empty string is truthy), 'for name in names' iterates characters, and four rulings keyed 'o','k','t','a' (names 'O','k','t','a', call 'out', bulk true) were written to vendor_scope_decisions.json with response {'ok': True, 'message': '4 vendor(s) left off the board'}. Rulings are by design permanent and never re-asked. Expected: reject non-list names.

### [admin/low] act_set_family without a title silently writes a 'null' key into family_overrides.json
scripts/admin.py:921-937. Only 'family' is validated; 'title' is not. Input {"family": "gtm"} (no title): over[None] is written, json.dump coerces the None key, and family_overrides.json gains {"null": {"family": "gtm", ...}} with response {'ok': True, 'message': 'None -> GTM (sales, marketing, BD)'}. Empty-string titles are also accepted. roles.py reads these overrides, so the junk key persists in a data file that teaches the classifier. Expected: refuse a missing/blank title.

### [admin/low] _probe re-reads and re-parses discovery_log.json for every company row; /api/triage takes 2.6s, /api/queues 1.5s
scripts/admin.py:207-208: _probe calls read('discovery_log.json') (197KB, 1646 entries) per company inside _board_rows, so q_boards and q_blocked each parse it ~1000+ times. Measured on real data (2111 companies): q_boards 0.72s, q_blocked 0.70s, /api/queues counts 1.5s, /api/triage 2.6s (triage additionally re-runs q_miscategorized/q_boards/q_vendor_scope after counting them at :448). admin.html refreshCounts() hits /api/queues after every single action click, so every ruling now pays ~1.5s. is_dismissed() has the same per-item re-read pattern but predates today. Expected: read the log once p

### [admin/low] act_dismiss on the miscategorized queue is accepted but has no effect (nested vs flat dismissal keys)
scripts/admin.py:147-149 stores dismissals nested (d[queue][key]) while q_miscategorized:529 checks the flat key f"miscategorized:{id}" that only act_place's keep-branch (:666) writes. Reproduced: act_dismiss({"queue": "miscategorized", "key": "acme"}) returned {'ok': True, 'message': 'dismissed'} and wrote admin_dismissed.json, yet acme remained in q_miscategorized; act_place({"id": "acme", "keep": true}) is what actually removes it. Latent today (RENDER.miscategorized uses /api/place, not dismissBtn), but the API silently no-ops.

### [admin/low] 'Also file here' removes the miscategorized card client-side but the item returns on every reload
admin.html:527-533 (also.onclick does n.remove() after /api/also) vs scripts/admin.py q_miscategorized:519-540, which considers neither 'also' entries nor any record of the action — the company's primary category is still 'Suppliers & Services'. Reproduced: after act_also moved acme into Public Safety / Fire (write verified in companies.json), q_miscategorized still listed acme. The row will be re-asked indefinitely unless moved or kept. Related polish in the same renderer: 'File it here' sends proposed as the JS template string 'null / null' when no proposal exists, which act_place stores ver

### [admin/low] Vendors tab offers bulk All in/All out buttons for the 'Everything else' group, which the design says cannot be bulk-ruled
admin.html:558-583 renders the group header with bulk buttons for every theme change, with no exclusion for 'Everything else'; scripts/admin.py q_vendor_scope's comment ('"Everything else" is not a family and cannot be bulk-ruled') and triage's rulable count (:456, which excludes it) both assume otherwise. Reproduced server-side that the theme reaches the client: q_vendor_scope returned ('Zeta Widgets', 'Everything else') and the sort places the group last, where the header + bulk buttons still render. One click bulk-rules an explicitly unrelated grab-bag (243-item real queue) in a permanent d

### [data/medium] 1973 suppliers.json rows missing govtech/vendor_type/source/researched keys entirely
suppliers.json has two schemas: 2777 rows carry all 14 keys, 1973 rows lack the keys govtech, vendor_type, source, and researched altogether (not null — absent). Examples: 1government-procurement-alliance, abm-industries-inc, ambassador-education-solutions. Any filter reading row['govtech'] raises KeyError or, via .get(), treats 1973 rows as govtech=None rather than false. Related: the govtech=true-supplier check found 0 rows with govtech==true (2777 false, 1973 key absent).

### [data/medium] suppliers.website mixes null and empty string: 2433 null vs 471 ""
suppliers.json website: 1846 real values, 2433 null, 471 empty-string — the only field that mixes both sentinels within one file (empty-str examples: 24pet, 911-petchip, acf-animal-rescue-pakistan; null examples: 10-s-tennis-supply-fast-dry-courts, 1finity-americas-inc, 1gpa). A 'missing website' filter testing only one sentinel misses the other bucket. Cross-file convention split, same hazard when joined: board.postings.location uses "" for missing (961 empty-str, 0 null) while companies.json/board.organizations location uses null (392 null, 0 empty-str); companies hiring.note uses "" (115) w

### [data/low] 44 descriptions carry superseded event tag 'ICMA 2026' (only in prior_tags)
Tag extraction per selftest.py regex ('exhibited at ...', comma-split) over companies+suppliers vs conferences.json (118 conferences, 118 event_tags): 0 occurrences of tags NEVER issued (event_tag ∪ prior_tags covers everything). But 1 distinct tag appears only in prior_tags: 'ICMA 2026' in 44 rows (examples: suppliers:4leaf, suppliers:baker-tilly, suppliers:bureau-veritas-north-america) — the catalog's current tag is 'ICMA 2025' with 'ICMA 2026' listed as a prior_tag, per the tag_note this is intentionally tolerated, but those 44 descriptions reference a tag the catalog no longer issues.

### [data/low] 3 of 4 ruling files do not exist on disk
In /private/tmp/claude-501/-Users-wyethwest/bae54666-b02f-4b55-98ed-4e5a5f866107/scratchpad/dock-work/data: scope_decisions.json present (9 entries); vendor_scope_decisions.json, placement_rulings.json, and admin_dismissed.json are MISSING. scripts/admin.py reads each with a {} default and creates them on first write, so absence means zero rulings/dismissals recorded, not corruption — but CLAUDE.md line 65 asserts dismissals 'are recorded' in admin_dismissed.json, which currently cannot be true.

### [data/low] Clean checks: 5 audits returned exactly zero violations
(1) board.organizations vs companies.json: 2111 vs 2111, ids match 1:1 — 0 orgs missing from companies, 0 companies missing from board, and all 4273 posting company_id values resolve to companies.json. (2) ats.ref empty/null while ats.type != 'unknown': 0 in companies.json, 0 in suppliers.json (all 4750 supplier ats.ref are null but all their ats.type are 'unknown'). (3) hiring.status outside {Yes, Sales (non-AE), None found, Unknown}: 0 in companies, 0 in suppliers. (4) 'also' naming its own primary sector/category: 0 — only 1 'also' exists in the entire dataset (tyler-technologies, primary G

### [intake/medium] norm() misses accented spelling variants (strips instead of transliterating) -> duplicate rows
scripts/conference_intake.py:40 and scripts/promote_candidates.py:44. `re.sub(r'[^a-z0-9 ]',' ', name.lower())` deletes accented letters rather than folding them, so the accented and plain spellings of the same company produce different dedup keys. REPRODUCED with norm(): norm('Motörhead Data')='mot rhead data' != norm('Motorhead Data')='motorhead data'; norm('Café Solutions')='caf solutions' != norm('Cafe Solutions')='cafe solutions'; norm('Cúram by Merative')='c ram by merative' != norm('Curam by Merative'). A researched candidate typed without the accent (or vice-versa) will not match the o

### [intake/medium] Three inconsistent slug/id functions across the three intake doors
add_company.py:256 uses `re.sub(r'[^a-z0-9]+','-',name.lower()).strip('-')` (no dedup-of-dashes beyond the + run, no parenthetical removal); conference_intake.py:46 kebab() collapses dashes but KEEPS parentheticals; promote_candidates.py:49 kebab() REMOVES parentheticals first. REPRODUCED: for 'ContentActive (ePollBooks.app)' the promote slug is 'contentactive' while the conference/add_company slug is 'contentactive-epollbooks-app'; for '<script>alert(1)</script>' promote yields 'script-alert-script' vs 'script-alert-1-script'. Same company entering through different doors therefore gets diffe

### [intake/medium] add_event() uses str.replace() and corrupts the base description / doubles notes when the event token recurs
scripts/conference_intake.py:59 `return desc.replace(m.group(1), m.group(1) + ', ' + event)`. .replace() rewrites EVERY occurrence of the captured event-list substring in the whole description, not just the one after 'exhibited at'. REPRODUCED: add_event('GFOA budgeting suite - exhibited at GFOA','NIGP 2026') returns 'GFOA, NIGP 2026 budgeting suite - exhibited at GFOA, NIGP 2026' (the leading 'GFOA' in the product description is corrupted). add_event('exhibited at PWX 2026. Note: exhibited at PWX 2026','GFOA 2026') appends to BOTH clauses. Triggers when a short conference acronym used as the 

### [intake/low] add_company dedup is website-host substring only — no name dedup, plus substring false positives
scripts/add_company.py:235-236 `dupe = next((c ... if host and host in (c.get('website') or '')))`. There is no name/norm-based dedup at all, so name variants with distinct URLs enter freely; the existing dataset already carries such pairs (companies.json has both 'Oracle' id='oracle' and 'Oracle Corporation' id='oracle-corporation'; 'Caterpillar' and 'Caterpillar Inc.' as separate rows even though they share the same website). Separately, the `host in website` substring test yields false positives: host 'a.com' matches 'https://banana.com' and 'id.me' matches 'https://rapid.me' (REPRODUCED), 

### [consistency/medium] admin.validate() is a strict subset of selftest's invariants despite three claims of parity
admin.py:15, admin.py:100, and CLAUDE.md:52-53 all state every admin write is validated 'against the same invariants selftest.py enforces'. Two selftest invariants are missing from validate() (admin.py:99-141): (1) year_founded must be an int in 1800-2100 (selftest.py:175-177) — reproduced: setting companies[0]["year_founded"] = "2003" (a string, what an HTML form sends) returns None from admin.validate but fails selftest; act_patch explicitly allows year_founded edits (admin.py:811-812). (2) the conference event-tag catalog check (selftest.py:184-211: every 'exhibited at X' tag in a descripti

### [consistency/medium] portfolio-status.json says the site went public and stale deployments were deleted; DEPLOY.md says those exact steps remain undone
portfolio-status.json:9 (session_summary: 'Site went public: Access gate removed and both whole-repo deployments deleted first') and :11 (recent_work: 'solesourcejobs.com is public - Access application deleted, stale whole-repo deployments removed') vs DEPLOY.md:44-57 which still instructs, as future owner actions: delete deployments eaffc723/5394c974 (:46), flip repo visibility to Public (:50), delete the Access application (:55), and DEPLOY.md:85 closes with 'which is the correct reason it has not been done'. portfolio-status.json:20 even contradicts its own summary ('Repo visibility flip to

### [consistency/medium] vercel.json is a stale second deploy door: its headers apply nowhere and a Vercel deploy would break /api/submit
vercel.json (added 78abf75, 2026-08-20, 'Nothing is deployed yet') configures buildCommand/outputDirectory/cleanUrls plus Cache-Control on /data/* and nosniff/Referrer-Policy/X-Frame-Options. DEPLOY.md §2/§4 (DEPLOY.md:20-42, dated 2026-08-21/23) commits to Cloudflare Pages as 'deliberately only one way', where vercel.json is inert — and build_site.py writes no _headers file into public/, so none of those headers exist on the documented deploy. Conversely, functions/api/submit.js uses the Cloudflare Pages Functions convention (onRequestPost, functions/ dir), which Vercel never executes, so dep

### [consistency/medium] CLAUDE.md is stale on three counts: queue list, ats.type list, and roadmap sectors
(1) CLAUDE.md:48-49 'Seven queues: duplicates, missing websites, missing boards, wrong placement, unclassified roles, acquisitions, website review' vs admin.py:662-664 QUEUES which has eleven (adds miscategorized/'Wrong bucket' — added in today's HEAD commits — plus vendors, scope, submissions). (2) CLAUDE.md:248-250 hand-lists ats.type as 13 values + unknown, omitting paylocity and oracle which scripts/ats.py:372 FETCHERS contains (15 fetchers, verified by import) — the exact hand-list drift the derived-set comments in selftest.py:24-28 and admin.py:48-52 were written to prevent, now living i

### [consistency/medium] Company/supplier id namespace has no gate anywhere, and collisions already exist
promote_candidates.py:66 treats companies ∪ suppliers ids as one namespace (taken_ids) and CLAUDE.md:246 says ids drop parenthetical suffixes — but conference_intake.py:46-47 kebab() does NOT strip parentheticals (promote_candidates.py:49-51 does), and neither script collision-checks new supplier ids. Verified in data: suppliers.json has 3 duplicated ids (lsi, local-government-hispanic-network, pars) and 2 ids colliding with companies.json ('hach' = company 'Hach (Veralto)' vs supplier 'Hach'; 'careers-in-government' in both files). No gate catches any of it: admin.validate checks company ids 

### [consistency/low] promote_candidates claims to 'land atomically' but truncate-writes; conference_intake writes companies.json with no validation at all
promote_candidates.py:160 comment: 'Validate the whole file the way admin does, then land atomically' — but lines 167-171 use plain Path.write_text on companies.json/suppliers.json/scope_review_queue.json, not the mkstemp+os.replace pattern of admin.write_atomic (admin.py:80-97), whose docstring names truncation mid-write as the failure to avoid. conference_intake.py:136-138 also writes companies.json and suppliers.json with write_text and never calls validate() (it only edits descriptions, but that is exactly the field selftest polices). Additionally conference_intake.py:80 takes `event = a.e

### [consistency/low] Checked and consistent (no disagreement found)
For completeness of the requested diffs: STATUSES copies agree byte-for-byte (admin.py:54 == selftest.py:21; export_xlsx.py:26-27 STATUS_COLOR covers exactly those four). ATS_TYPES are both derived from ats.FETCHERS (admin.py:53, selftest.py:29) so cannot drift from each other. The SUFFIX regex and norm() in conference_intake.py:36-43 vs promote_candidates.py:40-46 are identical. functions/api/submit.js:19 REPO="westjw/govtech-dock" matches the actual origin remote (git@github.com:westjw/govtech-dock.git), its fallback template add-company.yml exists at .github/ISSUE_TEMPLATE/add-company.yml, 
